# План разработки финансового бота

## 📊 Прогресс

| Итерация | Статус | Описание | Дата |
|----------|--------|----------|------|
| Итерация 1 | ✅ Завершено | Инициализация проекта | 2025-11-13 |
| Итерация 2 | ✅ Завершено | Базовый Telegram-бот | 2025-11-13 |
| Итерация 3 | ✅ Завершено | Интеграция LLM | 2025-11-13 |
| Итерация 4 | ✅ Завершено | Извлечение транзакций | 2025-11-13 |
| Итерация 5 | ✅ Завершено | Отчет о балансе | 2025-11-13 |
| Итерация 6 | ⏳ Ожидание | Улучшения и финализация | - |

**Легенда:**
- ⏳ Ожидание
- 🔄 В процессе
- ✅ Завершено
- ❌ Отменено

---

## Итерация 1: Инициализация проекта

**Цель:** Создать базовую структуру проекта

### Задачи:
- [x] Создать структуру директорий (`src/`, `docs/`)
- [x] Создать `pyproject.toml` с зависимостями
- [x] Создать `.env.example` с шаблоном переменных
- [x] Создать `.gitignore`
- [x] Создать `Makefile` с командами (install, run, clean)
- [x] Создать `README.md` с инструкцией по запуску

### Проверка:
```bash
make install  # Должно установить зависимости через uv
```

---

## Итерация 2: Базовый Telegram-бот

**Цель:** Запустить бота, который отвечает на сообщения

### Задачи:
- [x] Создать класс `FinanceBot` в `src/bot.py`
- [x] Инициализация aiogram (Bot, Dispatcher)
- [x] Загрузка переменных из `.env`
- [x] Хэндлер для текстовых сообщений (эхо-бот)
- [x] Метод `run()` для запуска polling
- [x] Базовое логирование (INFO, ERROR)

### Проверка:
```bash
make run  # Бот запускается
# Отправить сообщение боту → получить эхо-ответ
```

---

## Итерация 3: Интеграция LLM

**Цель:** Бот отвечает через LLM (OpenRouter)

### Задачи:
- [x] Инициализация OpenAI client с OpenRouter base_url
- [x] Системный промпт (константа в коде)
- [x] История диалога (список сообщений в памяти)
- [x] Метод для вызова LLM
- [x] Обработка ошибок API
- [x] Отправка ответа LLM пользователю

### Проверка:
```bash
make run  # Бот запускается
# Отправить "Привет" → получить ответ от LLM
# Проверить контекст диалога (задать уточняющий вопрос)
```

---

## Итерация 4: Извлечение транзакций

**Цель:** LLM извлекает транзакции из сообщений

### Задачи:
- [x] Таблица транзакций (список словарей в памяти)
- [x] Обновить системный промпт (инструкция для извлечения транзакций)
- [x] Парсинг ответа LLM (извлечение JSON с транзакциями)
- [x] Сохранение транзакций в таблицу
- [x] Подтверждение пользователю о записи транзакции

### Проверка:
```bash
make run  # Бот запускается
# Отправить "Купил продукты на 2500 рублей"
# Бот должен подтвердить: "✅ Записал расход: 2500₽ на продукты"
# Отправить "Получил зарплату 100000"
# Бот должен записать доход
```

---

## Итерация 5: Отчет о балансе

**Цель:** Бот показывает баланс и статистику

### Задачи:
- [x] Метод для расчета баланса (доходы - расходы)
- [x] Метод для форматирования отчета
- [x] Обработка запросов типа "покажи баланс", "мой баланс"
- [x] Отчет: общий баланс, доходы, расходы
- [x] Группировка по категориям (опционально)

### Проверка:
```bash
make run  # Бот запускается
# Добавить несколько транзакций
# Отправить "Покажи мой баланс"
# Получить отчет с балансом, доходами, расходами
```

---

## Итерация 6: Улучшения и финализация

**Цель:** Доработать UX и стабильность

### Задачи:
- [ ] Улучшить системный промпт (точность извлечения)
- [ ] Добавить команду `/start` с приветствием
- [ ] Добавить команду `/help` с инструкцией
- [ ] Улучшить обработку ошибок (понятные сообщения)
- [ ] Проверить все сценарии работы
- [ ] Обновить README.md с примерами использования

### Проверка:
```bash
make run  # Бот запускается
# Проверить /start, /help
# Проверить различные форматы сообщений о тратах
# Проверить запрос баланса
# Проверить обработку ошибок (неверный API ключ и т.п.)
```

---

## Будущие итерации (вне MVP)

- 🔮 Голосовые сообщения (транскрибация)
- 🔮 Обработка изображений (OCR чеков)
- 🔮 Экспорт данных (CSV, Excel)
- 🔮 Персистентность (сохранение в файл/БД)
- 🔮 Расширенная аналитика (графики, тренды)

---

## Правила работы

1. ✅ Выполнять итерации последовательно
2. ✅ Тестировать после каждой итерации
3. ✅ Обновлять прогресс в таблице
4. ✅ Отмечать выполненные задачи чекбоксами
5. ✅ Не переходить к следующей итерации без подтверждения
6. ✅ Следовать принципам из `conventions.md`

