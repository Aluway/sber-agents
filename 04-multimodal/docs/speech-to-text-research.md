# Исследование подходов к транскрибации голосовых сообщений

**Дата:** 13 ноября 2025  
**Цель:** Выбрать оптимальный подход для транскрибации голосовых сообщений Telegram в Finance Bot

---

## 1. OpenAI Whisper API (Облачный)

### Описание
Облачный API от OpenAI для транскрибации аудио. Модель Whisper - одна из лучших для Speech-to-Text.

### Плюсы
- ✅ Отличное качество распознавания (лучшее на рынке)
- ✅ Поддержка 99+ языков, включая русский
- ✅ Простая интеграция (OpenAI Python SDK)
- ✅ Не требует локальных ресурсов
- ✅ Автоматическое определение языка
- ✅ Быстрая обработка (~10-30 сек на минуту аудио)
- ✅ Форматы: mp3, mp4, mpeg, mpga, m4a, wav, webm

### Минусы
- ❌ Платный сервис ($0.006 за минуту аудио)
- ❌ Требует интернет
- ❌ Зависимость от внешнего сервиса
- ❌ Передача голосовых данных на сторонний сервер

### Сложность интеграции
**Очень простая** (1/5)

```python
from openai import OpenAI

client = OpenAI(api_key="...")
with open("voice.ogg", "rb") as audio:
    transcript = client.audio.transcriptions.create(
        model="whisper-1",
        file=audio,
        language="ru"
    )
print(transcript.text)
```

### Производительность
- **Скорость:** ~10-30 секунд на минуту аудио
- **Точность:** 95%+ для русского языка
- **Масштабируемость:** Отличная (облако)

### Стоимость
- **$0.006 за минуту** аудио
- Пример: 100 голосовых по 30 сек = 50 минут = **$0.30**
- Пример: 1000 сообщений/месяц ≈ **$3-5**

---

## 2. Локальный Whisper (openai-whisper)

### Описание
Официальная Python библиотека OpenAI Whisper для локального запуска.

### Плюсы
- ✅ Бесплатно (только локальные ресурсы)
- ✅ Отличное качество (та же модель что в API)
- ✅ Полная конфиденциальность
- ✅ Нет зависимости от интернета
- ✅ Поддержка русского языка
- ✅ 5 размеров моделей (tiny, base, small, medium, large)

### Минусы
- ❌ Требует GPU для нормальной скорости
- ❌ Медленно на CPU (2-5 минут на минуту аудио)
- ❌ Требует ~1-10 GB VRAM (зависит от модели)
- ❌ Сложнее установка (PyTorch, CUDA)
- ❌ Потребляет ресурсы сервера

### Сложность интеграции
**Средняя** (3/5)

```python
import whisper

model = whisper.load_model("base")  # tiny, base, small, medium, large
result = model.transcribe("voice.ogg", language="ru")
print(result["text"])
```

### Производительность
**На CPU:**
- tiny: ~30 сек/мин аудио, точность 70%
- base: ~1 мин/мин аудио, точность 80%
- small: ~2 мин/мин аудио, точность 85%
- medium: ~5 мин/мин аудио, точность 90%

**На GPU (V100):**
- tiny: ~3 сек/мин аудио
- base: ~5 сек/мин аудио
- small: ~10 сек/мин аудио
- medium: ~20 сек/мин аудио

### Стоимость
- **$0** - бесплатно
- Требует GPU для нормальной работы

---

## 3. faster-whisper (Локальный, оптимизированный)

### Описание
Оптимизированная версия Whisper с использованием CTranslate2. В 4x быстрее оригинала при том же качестве.

### Плюсы
- ✅ **В 4 раза быстрее** openai-whisper
- ✅ **Меньше потребление памяти** (~2x)
- ✅ То же качество распознавания
- ✅ Бесплатно
- ✅ Поддержка русского языка
- ✅ Работает на CPU приемлемо
- ✅ Простая установка

### Минусы
- ❌ Всё равно медленно на CPU (30 сек - 1 мин на минуту аудио)
- ❌ Требует локальные ресурсы
- ❌ Нужна установка зависимостей

### Сложность интеграции
**Средняя** (3/5)

```python
from faster_whisper import WhisperModel

model = WhisperModel("base", device="cpu", compute_type="int8")
segments, info = model.transcribe("voice.ogg", language="ru")
text = " ".join([segment.text for segment in segments])
print(text)
```

### Производительность
**На CPU:**
- base: ~15-30 сек/мин аудио, точность 80%
- small: ~30-60 сек/мин аудио, точность 85%

**На GPU:**
- base: ~2-3 сек/мин аудио
- small: ~5 сек/мин аудио

### Стоимость
- **$0** - бесплатно
- Рекомендуется GPU

---

## 4. Vosk (Локальный, легковесный)

### Описание
Оффлайн библиотека распознавания речи. Работает на CPU без проблем.

### Плюсы
- ✅ Быстро работает даже на CPU
- ✅ Малый размер моделей (~50 MB - 1.8 GB)
- ✅ Низкое потребление памяти
- ✅ Бесплатно и open source
- ✅ Поддержка русского языка
- ✅ Не требует GPU
- ✅ Простая установка

### Минусы
- ❌ Качество хуже чем Whisper (~70-80% точность)
- ❌ Хуже с разными акцентами
- ❌ Меньше языковых моделей
- ❌ Может требовать post-processing

### Сложность интеграции
**Простая** (2/5)

```python
from vosk import Model, KaldiRecognizer
import wave

model = Model("model-ru")  # Скачать модель заранее
wf = wave.open("voice.wav", "rb")
rec = KaldiRecognizer(model, wf.getframerate())

while True:
    data = wf.readframes(4000)
    if len(data) == 0:
        break
    rec.AcceptWaveform(data)

result = json.loads(rec.FinalResult())
print(result["text"])
```

### Производительность
- **Скорость на CPU:** ~5-10 сек на минуту аудио
- **Точность:** 70-80% для русского
- **Потребление памяти:** 200-500 MB

### Стоимость
- **$0** - бесплатно
- **Модель:** ~50-200 MB (маленькая), ~1.8 GB (большая)

---

## 5. AssemblyAI (Облачный)

### Описание
Специализированный сервис для транскрибации с расширенными функциями.

### Плюсы
- ✅ Высокое качество
- ✅ Простой REST API
- ✅ Дополнительные функции (sentiment analysis, PII redaction)
- ✅ Хорошая документация
- ✅ Поддержка русского языка

### Минусы
- ❌ Платный ($0.00025 за секунду = $0.015 за минуту)
- ❌ **Дороже чем OpenAI** в 2.5 раза
- ❌ Требует интернет
- ❌ Меньше популярен для русского

### Сложность интеграции
**Простая** (2/5)

### Производительность
- **Скорость:** 15-30 секунд
- **Точность:** 90-95% для английского, ~85% для русского

### Стоимость
- **$0.015 за минуту** аудио
- Пример: 1000 сообщений/месяц ≈ **$7-10**

---

## 6. Yandex SpeechKit (Облачный, для русского)

### Описание
Облачный сервис Яндекса, оптимизированный для русского языка.

### Плюсы
- ✅ **Лучшее качество для русского языка**
- ✅ Учитывает русские особенности (акценты, диалекты)
- ✅ Простая интеграция
- ✅ Хорошая документация на русском
- ✅ Streaming и batch режимы
- ✅ Дополнительные функции (нормализация чисел, дат)

### Минусы
- ❌ Платный (~1₽ за 15 секунд = ~4₽ за минуту)
- ❌ Требует интернет
- ❌ Только для российских пользователей
- ❌ Нужна привязка карты
- ❌ Ограниченная бесплатная квота

### Сложность интеграции
**Средняя** (3/5)

```python
import requests

IAM_TOKEN = "..."
folder_id = "..."

with open("voice.ogg", "rb") as f:
    data = f.read()

response = requests.post(
    "https://stt.api.cloud.yandex.net/speech/v1/stt:recognize",
    headers={"Authorization": f"Bearer {IAM_TOKEN}"},
    params={"lang": "ru-RU", "folderId": folder_id},
    data=data
)
result = response.json()
print(result["result"])
```

### Производительность
- **Скорость:** 5-15 секунд
- **Точность:** 95%+ для русского языка

### Стоимость
- **~4₽ за минуту** аудио (~$0.04)
- **Бесплатно:** первые 10,000 запросов в месяц (для тестирования)
- Пример: 1000 сообщений/месяц ≈ **2000₽** ($20)

---

## 7. Telegram Bot API (встроенные возможности)

### Описание
Telegram не предоставляет встроенную транскрибацию, но можно использовать их серверы для хранения файлов.

### Возможности
- ✅ Получение голосового сообщения (file_id)
- ✅ Скачивание файла в формате .ogg (Opus codec)
- ❌ **Нет встроенной транскрибации**

### Интеграция
```python
@dp.message(lambda m: m.voice is not None)
async def handle_voice(message: Message):
    voice = message.voice
    file = await bot.get_file(voice.file_id)
    voice_bytes = await bot.download_file(file.file_path)
    
    # Далее нужно передать в STT сервис
```

---

## 8. Мультимодальные LLM с Speech-to-Text

### Описание
Некоторые современные LLM умеют работать с аудио напрямую.

### Примеры
- **GPT-4o** (OpenAI) - поддерживает аудио input
- **Gemini 1.5** (Google) - поддерживает аудио
- Пока не доступны в Ollama

### Плюсы
- ✅ Единый API для текста, изображений и аудио
- ✅ Высокое качество
- ✅ Можно сразу извлекать транзакции

### Минусы
- ❌ Очень дорого
- ❌ Только облачные решения
- ❌ Пока мало моделей поддерживают

### Сложность интеграции
**Простая** (2/5) - если модель поддерживает

### Стоимость
- **GPT-4o:** ~$0.15 за минуту (очень дорого)

---

## 📊 Сравнительная таблица

| Подход | Качество (RU) | Скорость | Стоимость/мин | Сложность | Локально |
|--------|---------------|----------|---------------|-----------|----------|
| **OpenAI Whisper API** | ⭐⭐⭐⭐⭐ | ⚡⚡⚡ | $0.006 | ⭐ | ❌ |
| **Whisper локально** | ⭐⭐⭐⭐⭐ | ⚡ (CPU) / ⚡⚡⚡ (GPU) | $0 | ⭐⭐⭐ | ✅ |
| **faster-whisper** | ⭐⭐⭐⭐⭐ | ⚡⚡ (CPU) / ⚡⚡⚡⚡ (GPU) | $0 | ⭐⭐⭐ | ✅ |
| **Vosk** | ⭐⭐⭐ | ⚡⚡⚡⚡ | $0 | ⭐⭐ | ✅ |
| **AssemblyAI** | ⭐⭐⭐⭐ | ⚡⚡⚡ | $0.015 | ⭐⭐ | ❌ |
| **Yandex SpeechKit** | ⭐⭐⭐⭐⭐ | ⚡⚡⚡⚡ | $0.04 | ⭐⭐⭐ | ❌ |
| **GPT-4o (audio)** | ⭐⭐⭐⭐⭐ | ⚡⚡⚡ | $0.15 | ⭐ | ❌ |

---

## 🎯 Рекомендация для личного проекта Finance Bot

### ⭐ **Рекомендуемый подход: OpenAI Whisper API**

**Почему:**

1. **Простота интеграции** ⭐⭐⭐⭐⭐
   - 5 строк кода
   - Уже используем OpenAI client
   - Не нужны дополнительные зависимости

2. **Качество для русского языка** ⭐⭐⭐⭐⭐
   - Лучшее распознавание русской речи
   - Автоопределение языка
   - Хорошо с разными акцентами

3. **Стоимость** ⭐⭐⭐⭐
   - Для личного использования: **$1-3 в месяц**
   - Самый дешевый среди облачных (дешевле Yandex и AssemblyAI)
   - Не нужно GPU на сервере

4. **Производительность** ⭐⭐⭐⭐
   - Быстрая обработка (10-30 сек)
   - Пользователь не ждет долго
   - Не нагружает наш сервер

---

## 🥈 Альтернативный подход: faster-whisper (если есть GPU)

Если у тебя есть Tesla V100 с установленным драйвером:

**Плюсы:**
- ✅ Бесплатно
- ✅ Быстро на GPU (~5 сек)
- ✅ Хорошее качество
- ✅ Приватность

**Минусы:**
- ❌ Нужен CUDA драйвер
- ❌ Сложнее установка
- ❌ Нагружает сервер

**Рекомендация:** Только если планируешь много голосовых (>10,000/месяц)

---

## 🥉 Бюджетный вариант: Vosk

Если критично важна бесплатность:

**Плюсы:**
- ✅ Полностью бесплатно
- ✅ Работает на CPU
- ✅ Простая установка

**Минусы:**
- ❌ Качество хуже (~70-80%)
- ❌ Могут быть ошибки в цифрах

**Рекомендация:** Только для тестирования или если критично важна бесплатность

---

## 💡 Итоговая рекомендация

### Для Finance Bot: **OpenAI Whisper API**

**Обоснование:**
1. **Простота** - интеграция за 10 минут
2. **Качество** - критично для распознавания сумм и категорий
3. **Стоимость** - приемлемая для личного проекта ($1-3/месяц)
4. **Надежность** - стабильный сервис от OpenAI
5. **Уже используем OpenAI SDK** - не нужны новые зависимости

### Пример интеграции

```python
# Добавить в __init__
self.whisper_client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# Хэндлер для голосовых сообщений
async def handle_voice(self, message: Message):
    # Скачиваем голосовое
    voice = message.voice
    file = await self.bot.get_file(voice.file_id)
    voice_bytes = await self.bot.download_file(file.file_path)
    
    # Транскрибируем через Whisper
    transcript = self.whisper_client.audio.transcriptions.create(
        model="whisper-1",
        file=("voice.ogg", voice_bytes, "audio/ogg"),
        language="ru"
    )
    
    # Обрабатываем как текстовое сообщение
    response = await self.call_llm(transcript.text)
    await message.answer(f"🎤 Распознано: {transcript.text}\n\n{response}")
```

---

## 📝 План реализации

1. ✅ Добавить `OPENAI_API_KEY` в `.env` (если используем Whisper API)
2. ✅ Зарегистрировать хэндлер для `message.voice`
3. ✅ Скачивать голосовое сообщение
4. ✅ Отправлять в Whisper API
5. ✅ Получать транскрипцию
6. ✅ Передавать в существующий `call_llm()`

**Время реализации:** ~20-30 минут  
**Сложность:** Низкая (используем уже знакомый OpenAI SDK)

---

## 🔄 Возможность переключения

Можно сделать гибридный подход:

```python
# В .env
USE_LOCAL_WHISPER=false  # true для локального, false для API

# В коде
if os.getenv("USE_LOCAL_WHISPER") == "true":
    # faster-whisper локально
else:
    # OpenAI Whisper API
```

Это позволит переключаться между вариантами без изменения кода.

---

*Исследование завершено: 2025-11-13*

